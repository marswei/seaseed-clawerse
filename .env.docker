# Docker 环境变量配置
NODE_ENV=production
PORT=3000

# 站点域名
SITE_DOMAIN=http://localhost:3000

# MySQL 配置（请修改为你的配置）
DB_HOST=db
DB_USER=your_db_user
DB_PASSWORD=your_db_password
DB_NAME=seaseed

# API Token（请修改为安全的随机字符串）
API_TOKEN=your-secure-random-token

# 超级管理员Token（可选）
SUPER_TOKEN=
